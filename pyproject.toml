[project]
name = "mp3norm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "xdream oldlu", email = "xdream@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pyfiglet>=1.0.4",
    "rich>=15.0.0",
    "tqdm>=4.70.0",
]

[project.scripts]
mp3norm = "mp3norm:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
